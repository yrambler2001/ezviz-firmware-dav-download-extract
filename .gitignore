# The .dav image is vendored deliberately - see README "Legal".
# Extracted artifacts — regenerate any time with ./unpack-dav.sh
firmware/unpacked/
firmware/rootfs/
unpacked/
rootfs/

# Never commit credentials.
.env
*.env
credentials*
secrets*
*password*

# macOS
.DS_Store
